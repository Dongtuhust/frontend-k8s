$(document).ready(function(){
 
    // show html form when 'update order' button was clicked
    $(document).on('click', '.update-order-button', function(){
        // get order id
		var id = $(this).attr('data-id');
    });
     
    // 'update order form' submit handle will be here
});